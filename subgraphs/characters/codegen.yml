overwrite: true
schema:
  - "./schema.graphql"
documents: null # "./graphql/schema/index.ts"?
generates:
  ./src/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
