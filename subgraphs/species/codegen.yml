overwrite: true
schema:
  - "./schema.graphql"
documents: null
generates:
  ./src/types/schemaTypes.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
